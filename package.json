{
  "name": "employee-task-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend start",
    "dev": "npm --prefix frontend start",
    "backend": "cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "axios": "^1.18.1"
  }
}
